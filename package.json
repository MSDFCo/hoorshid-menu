{
  "name": "hoorshid-menu",
  "version": "1.0.0",
  "private": true,
  "description": "Bilingual (FA/EN) QR menu website for Hoorshid restaurant — React + Vite frontend, Express + SQLite backend.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c yellow,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "node server/index.js",
    "seed": "node server/seed.js",
    "set-password": "node server/scripts/set-password.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
